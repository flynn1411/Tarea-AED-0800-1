Para agregar contenido:

1.Te metes en la carpeta y creas archivos o carpetas.
2.Terminal:
  git add --all
  git commit -m "Mensaje del commit"
  git push

Para actualizar contenido:

1.Tener la terminal en el directorio donde el .git esté ubicado
2.Terminal:
  git pull
